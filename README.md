# simple_weather_agent
A simple Python AI agent using the Groq API and the LLaMA-3.1-8B model. It follows a Plan → Action → Observe → Output workflow to process user queries and can call tools like a weather API to fetch real-time data before generating the final response.
